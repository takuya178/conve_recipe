 <% content_for(:title, t('.title')) %> 
  <div class="food_top">
    <div class="container">
      <%= render 'shared/component_btn', url: mains_path %>
      <%= paginate @mains %> 
      <div class="row">
        <% @mains.each do |main| %>
          <%= render 'shared/index', main: main, combi_path: main_path(main) %>
        <% end %>
      </div>
      <%= paginate @mains %> 
    </div>
  </div>  