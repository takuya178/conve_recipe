<% content_for(:title, t('.title')) %>
<div class="food_top">
  <div class="container"> 
    <% unless @sub.mains.empty? %>
    <div class="row">
      <% @sub.mains.each do |main| %>
        <%= render 'shared/show', sub: main %> 
      <% end %>
    </div>
    <% else %>
      <p>関連付けはありません</p>
    <% end %>
  </div>
</div>